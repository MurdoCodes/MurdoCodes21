# URL of your future Payload CMS instance (lib/payload.ts)
PAYLOAD_URL=http://localhost:3001

# URL of the standalone Express service (server/) that handles the contact form
EXPRESS_API_URL=http://localhost:3001
